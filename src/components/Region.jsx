import React from 'react';

const Region = ({ name }) => {
    return (
        <div>
            <h2>Region Details</h2>
            <p>Name: {name}</p>
        </div>
    );
};

export default Region;
